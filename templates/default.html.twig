{% extends 'partials/base.html.twig' %}
{% block content %}
    <div class="container">
        <div class="columns is-centered">
            <div class="column is-6">
                <div class="content shadow-md bm">
                    <h1 class="has-text-info">{{ page.title }}</h1>
                    <div class="has-text-justified">
                        {{ page.content }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
